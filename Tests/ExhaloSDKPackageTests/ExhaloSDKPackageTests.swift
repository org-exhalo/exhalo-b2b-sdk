import XCTest
@testable import ExhaloSDKPackage

final class ExhaloSDKPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExhaloSDKPackage().text, "Hello, World!")
    }
}
